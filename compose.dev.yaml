#
# docker-compose.yaml (DEV)
# https://github.com/COSI-Lab/
#

name: mirror
services:
  # --- Sync Scheduler ---
  sync:
    build: ./module/sync
    container_name: sync
    expose:
      - ${SYNC_MANUAL_SYNC_PORT}
    restart: unless-stopped
    environment:
      - DRY_RUN=${SYNC_DRY_RUN}
      - SPDLOG_LEVEL=${SYNC_SPDLOG_LEVEL}
      - MANUAL_SYNC_PORT=${SYNC_MANUAL_SYNC_PORT}
    volumes:
      - "${MIRROR_STORAGE}:/storage"
      - "./config/sync/configs:/mirror/configs:ro"
      - "./config/sync/scripts:/mirror/scripts:ro"
      - "./config/sync/secrets:/mirror/secrets:ro"
    networks:
      - mirror
    stdin_open: true
    tty: true
  # --- Web Proxy ---
  proxy:
    image: nginx:latest
    container_name: proxy
    ports:
      - "80:80"
    restart: unless-stopped
    volumes:
      - "./config/nginx.conf:/etc/nginx/conf.d/default.conf:ro"
      - "${MIRROR_STORAGE}:/storage:ro"
      - "./module/web/mirror_website/static:/var/www/static:ro"
    networks:
      - mirror
    stdin_open: true
    tty: true
  # --- Website ---
  web:
    build: ./module/web
    container_name: web
    expose:
      - 8000
    restart: unless-stopped
    environment:
      - DEBUG=false
      - SECRET_KEY=${WEB_DJANGO_SECRET}
    volumes:
      - "./config/sync/configs:/configs:ro"
    networks:
      - mirror
    stdin_open: true
    tty: true
  # --- Monitoring stack (Loki + Grafana + Alloy) ---
  alloy:
    image: grafana/alloy:v1.7.5
    container_name: alloy
    expose:
      - 4317
      - 4318
    volumes:
      - ./config/config.alloy:/etc/alloy/config.alloy
      - /var/run/docker.sock:/var/run/docker.sock
    command: run --storage.path=/var/lib/alloy/data /etc/alloy/config.alloy
    depends_on:
      - loki
    networks:
      - loki
  loki:
    image: grafana/loki:3.4.2
    container_name: loki
    expose:
      - 3100
    volumes:
      - ./config/loki-config.yaml:/etc/loki/local-config.yaml
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - loki
  grafana:
    image: grafana/grafana:11.6.0
    container_name: grafana
    # TODO: Enable auth
    environment:
      - GF_FEATURE_TOGGLES_ENABLE=grafanaManagedRecordingRules
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Admin
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_BASIC_ENABLED=false
    ports:
      - 127.0.0.1:3000:3000/tcp
    # TODO: Break out provisioning into a config file?
    # TODO: Persist grafana configuration (/var/lib/grafana)
    entrypoint:
       - sh
       - -euc
       - |
         mkdir -p /etc/grafana/provisioning/datasources
         cat <<EOF > /etc/grafana/provisioning/datasources/ds.yaml
         apiVersion: 1
         datasources:
         - name: Loki
           type: loki
           access: proxy
           orgId: 1
           url: 'http://loki:3100'
           basicAuth: false
           isDefault: true
           version: 1
           editable: true 
         EOF
         /run.sh
    networks:
      - loki

networks:
  mirror:
  loki:
